# Gooseherd — Docker Compose for Coolify deployment
#
# Coolify notes:
#   - ${VAR:?} = required (blocks deploy until set in Coolify UI)
#   - ${VAR:-default} = optional with default
#   - No `ports:` — Coolify's Traefik proxy handles external routing + SSL
#   - No `env_file:` — Coolify injects env vars via its own mechanism
#
# For local development, create a .env file and run:
#   docker compose --env-file .env up --build

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: gooseherd
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gooseherd}
      POSTGRES_DB: gooseherd
    volumes:
      - gooseherd-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gooseherd"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  gooseherd:
    image: ghcr.io/chocksy/gooseherd:latest
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # ── Database ──
      - DATABASE_URL=postgres://gooseherd:${POSTGRES_PASSWORD:-gooseherd}@postgres:5432/gooseherd
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      # ── Branding ──
      - APP_NAME=${APP_NAME:-Gooseherd}

      # ── Slack (optional — omit for dashboard-only mode) ──
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN:-}
      - SLACK_APP_TOKEN=${SLACK_APP_TOKEN:-}
      - SLACK_SIGNING_SECRET=${SLACK_SIGNING_SECRET:-}
      - SLACK_CLIENT_ID=${SLACK_CLIENT_ID:-}
      - SLACK_CLIENT_SECRET=${SLACK_CLIENT_SECRET:-}
      - SLACK_AUTH_REDIRECT_URI=${SLACK_AUTH_REDIRECT_URI:-}
      - SLACK_COMMAND_NAME=${SLACK_COMMAND_NAME:-gooseherd}
      - SLACK_ALLOWED_CHANNELS=${SLACK_ALLOWED_CHANNELS:-}

      # ── Required (GitHub — use EITHER PAT or App) ──
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      - GITHUB_APP_ID=${GITHUB_APP_ID:-}
      - GITHUB_APP_PRIVATE_KEY=${GITHUB_APP_PRIVATE_KEY:-}
      - GITHUB_APP_INSTALLATION_ID=${GITHUB_APP_INSTALLATION_ID:-}
      - GITHUB_DEFAULT_OWNER=${GITHUB_DEFAULT_OWNER:-}
      - REPO_ALLOWLIST=${REPO_ALLOWLIST:-}

      # ── Agent (any CLI — pi-agent, Codex, Claude Code, Cursor, Goose) ──
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - CODEX_API_KEY=${CODEX_API_KEY:-}
      - CURSOR_API_KEY=${CURSOR_API_KEY:-}
      - AGENT_COMMAND_TEMPLATE=${AGENT_COMMAND_TEMPLATE:-}
      - AGENT_FOLLOW_UP_TEMPLATE=${AGENT_FOLLOW_UP_TEMPLATE:-}
      - AGENT_TIMEOUT_SECONDS=${AGENT_TIMEOUT_SECONDS:-600}

      # ── Pipeline ──
      - PIPELINE_FILE=${PIPELINE_FILE:-pipelines/pipeline.yml}

      # ── Browser Verify ──
      - BROWSER_VERIFY_ENABLED=${BROWSER_VERIFY_ENABLED:-false}
      - SCREENSHOT_ENABLED=${SCREENSHOT_ENABLED:-false}
      - BROWSER_VERIFY_EXEC_TIMEOUT_MS=${BROWSER_VERIFY_EXEC_TIMEOUT_MS:-}
      - BROWSER_VERIFY_MODEL=${BROWSER_VERIFY_MODEL:-}
      - BROWSER_VERIFY_EXECUTION_MODEL=${BROWSER_VERIFY_EXECUTION_MODEL:-}
      - BROWSER_VERIFY_MAX_STEPS=${BROWSER_VERIFY_MAX_STEPS:-15}
      - REVIEW_APP_URL_PATTERN=${REVIEW_APP_URL_PATTERN:-}
      - BROWSER_VERIFY_TEST_EMAIL=${BROWSER_VERIFY_TEST_EMAIL:-}
      - BROWSER_VERIFY_TEST_PASSWORD=${BROWSER_VERIFY_TEST_PASSWORD:-}
      - BROWSER_VERIFY_EMAIL_DOMAINS=${BROWSER_VERIFY_EMAIL_DOMAINS:-}

      # ── Orchestrator ──
      - ORCHESTRATOR_ENABLED=${ORCHESTRATOR_ENABLED:-false}
      - ORCHESTRATOR_MODEL=${ORCHESTRATOR_MODEL:-}
      - ORCHESTRATOR_TIMEOUT_MS=${ORCHESTRATOR_TIMEOUT_MS:-}
      - ORCHESTRATOR_WALL_CLOCK_TIMEOUT_MS=${ORCHESTRATOR_WALL_CLOCK_TIMEOUT_MS:-}

      # ── Validation ──
      - VALIDATION_COMMAND=${VALIDATION_COMMAND:-}
      - LINT_FIX_COMMAND=${LINT_FIX_COMMAND:-}
      - LOCAL_TEST_COMMAND=${LOCAL_TEST_COMMAND:-}
      - MAX_VALIDATION_ROUNDS=${MAX_VALIDATION_ROUNDS:-2}

      # ── Runtime ──
      - RUNNER_CONCURRENCY=${RUNNER_CONCURRENCY:-1}
      - WORK_ROOT=${WORK_ROOT:-.work}
      - DATA_DIR=${DATA_DIR:-data}
      - DRY_RUN=${DRY_RUN:-false}

      # ── Git identity ──
      - BRANCH_PREFIX=${BRANCH_PREFIX:-gooseherd}
      - DEFAULT_BASE_BRANCH=${DEFAULT_BASE_BRANCH:-main}
      - GIT_AUTHOR_NAME=${GIT_AUTHOR_NAME:-Gooseherd Bot}
      - GIT_AUTHOR_EMAIL=${GIT_AUTHOR_EMAIL:-gooseherd-bot@local}

      # ── Dashboard ──
      - DASHBOARD_ENABLED=${DASHBOARD_ENABLED:-true}
      - DASHBOARD_HOST=0.0.0.0
      - DASHBOARD_PORT=${DASHBOARD_PORT:-8787}
      - DASHBOARD_PUBLIC_URL=${DASHBOARD_PUBLIC_URL:-}
      - DASHBOARD_TOKEN=${DASHBOARD_TOKEN:-}

      # ── Slack UX ──
      - SLACK_PROGRESS_HEARTBEAT_SECONDS=${SLACK_PROGRESS_HEARTBEAT_SECONDS:-20}
      - MAX_TASK_CHARS=${MAX_TASK_CHARS:-4000}
      - TEAM_CHANNEL_MAP=${TEAM_CHANNEL_MAP:-}

      # ── LLM (shared default for all LLM features, individual overrides optional) ──
      - DEFAULT_LLM_MODEL=${DEFAULT_LLM_MODEL:-}
      - PLAN_TASK_MODEL=${PLAN_TASK_MODEL:-}
      - SCOPE_JUDGE_ENABLED=${SCOPE_JUDGE_ENABLED:-false}
      - SCOPE_JUDGE_MODEL=${SCOPE_JUDGE_MODEL:-}
      - SCOPE_JUDGE_MIN_PASS_SCORE=${SCOPE_JUDGE_MIN_PASS_SCORE:-60}

      # ── Workspace Cleanup ──
      - WORKSPACE_CLEANUP_ENABLED=${WORKSPACE_CLEANUP_ENABLED:-true}
      - WORKSPACE_MAX_AGE_HOURS=${WORKSPACE_MAX_AGE_HOURS:-24}
      - WORKSPACE_CLEANUP_INTERVAL_MINUTES=${WORKSPACE_CLEANUP_INTERVAL_MINUTES:-30}

      # ── CI Feedback Loop ──
      - CI_WAIT_ENABLED=${CI_WAIT_ENABLED:-false}

      # ── CEMS Memory (optional) ──
      - CEMS_ENABLED=${CEMS_ENABLED:-false}
      - CEMS_API_URL=${CEMS_API_URL:-}
      - CEMS_API_KEY=${CEMS_API_KEY:-}
      - CEMS_TEAM_ID=${CEMS_TEAM_ID:-}

      # ── Pi-Agent Extensions ──
      - PI_AGENT_EXTENSIONS=${PI_AGENT_EXTENSIONS:-}
      - MCP_EXTENSIONS=${MCP_EXTENSIONS:-}

      # ── OpenRouter Provider Routing (JSON, e.g. {"ignore":["DeepInfra"]}) ──
      - OPENROUTER_PROVIDER_PREFERENCES=${OPENROUTER_PROVIDER_PREFERENCES:-}

      # ── Observer (auto-trigger from Sentry, GitHub, Slack) ──
      - OBSERVER_ENABLED=${OBSERVER_ENABLED:-false}
      - OBSERVER_ALERT_CHANNEL_ID=${OBSERVER_ALERT_CHANNEL_ID:-}
      - OBSERVER_RULES_FILE=${OBSERVER_RULES_FILE:-observer-rules/default.yml}
      - OBSERVER_WEBHOOK_PORT=${OBSERVER_WEBHOOK_PORT:-9090}
      - OBSERVER_WEBHOOK_SECRETS=${OBSERVER_WEBHOOK_SECRETS:-}
      - SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN:-}
      - SENTRY_ORG_SLUG=${SENTRY_ORG_SLUG:-}
      - OBSERVER_MAX_RUNS_PER_DAY=${OBSERVER_MAX_RUNS_PER_DAY:-50}
      - OBSERVER_MAX_RUNS_PER_REPO_PER_DAY=${OBSERVER_MAX_RUNS_PER_REPO_PER_DAY:-5}
      - OBSERVER_COOLDOWN_MINUTES=${OBSERVER_COOLDOWN_MINUTES:-60}
      - OBSERVER_GITHUB_WATCHED_REPOS=${OBSERVER_GITHUB_WATCHED_REPOS:-}
      - OBSERVER_GITHUB_WEBHOOK_SECRET=${OBSERVER_GITHUB_WEBHOOK_SECRET:-}
      - OBSERVER_SENTRY_WEBHOOK_SECRET=${OBSERVER_SENTRY_WEBHOOK_SECRET:-}
      - OBSERVER_SLACK_WATCHED_CHANNELS=${OBSERVER_SLACK_WATCHED_CHANNELS:-}
      - OBSERVER_SLACK_BOT_ALLOWLIST=${OBSERVER_SLACK_BOT_ALLOWLIST:-}
      - OBSERVER_REPO_MAP=${OBSERVER_REPO_MAP:-}
      - AUTONOMOUS_SCHEDULER_ENABLED=${AUTONOMOUS_SCHEDULER_ENABLED:-false}

      # ── Run Supervisor (auto-retry + watchdog) ──
      - SUPERVISOR_ENABLED=${SUPERVISOR_ENABLED:-true}
      - SUPERVISOR_RUN_TIMEOUT_SECONDS=${SUPERVISOR_RUN_TIMEOUT_SECONDS:-7200}
      - SUPERVISOR_NODE_STALE_SECONDS=${SUPERVISOR_NODE_STALE_SECONDS:-1800}
      - SUPERVISOR_WATCHDOG_INTERVAL_SECONDS=${SUPERVISOR_WATCHDOG_INTERVAL_SECONDS:-30}
      - SUPERVISOR_MAX_AUTO_RETRIES=${SUPERVISOR_MAX_AUTO_RETRIES:-1}
      - SUPERVISOR_RETRY_COOLDOWN_SECONDS=${SUPERVISOR_RETRY_COOLDOWN_SECONDS:-60}
      - SUPERVISOR_MAX_RETRIES_PER_DAY=${SUPERVISOR_MAX_RETRIES_PER_DAY:-20}

      # ── Sandbox (Docker-out-of-Docker) ──
      - SANDBOX_RUNTIME=${SANDBOX_RUNTIME:-local}
      - SANDBOX_ENABLED=${SANDBOX_ENABLED:-false}
      - SANDBOX_IMAGE=${SANDBOX_IMAGE:-gooseherd/sandbox:default}
      - SANDBOX_HOST_WORK_PATH=${SANDBOX_HOST_WORK_PATH:-}
      - SANDBOX_CPUS=${SANDBOX_CPUS:-2}
      - SANDBOX_MEMORY_MB=${SANDBOX_MEMORY_MB:-4096}

    volumes:
      # When SANDBOX_ENABLED=true, use bind mount so sandbox containers can share the path:
      #   SANDBOX_HOST_WORK_PATH=/data/gooseherd/work → /data/gooseherd/work:/app/.work
      # When SANDBOX_ENABLED=false, named volume is fine (default):
      - ${SANDBOX_HOST_WORK_PATH:-gooseherd-work}:/app/.work
      - gooseherd-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock   # Docker-out-of-Docker for sandbox containers
    restart: unless-stopped
    ports:
      - "${DASHBOARD_PORT:-8787}:8787"
      - "${OBSERVER_WEBHOOK_PORT:-9090}:9090"
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/healthz').then(r=>{process.exit(r.ok?0:1)}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

    # ── Traefik basic auth (uncomment and set your credentials) ──
    # Generate: htpasswd -nbB admin yourpassword
    # Double $$ signs in YAML to escape bcrypt hashes
    # labels:
    #   - 'traefik.http.middlewares.gooseherd-auth.basicauth.users=admin:$$2y$$05$$HASH_HERE'

  # Sandbox image — only built when the sandbox profile is active:
  #   docker compose --profile sandbox up --build
  sandbox:
    build: sandbox/
    image: gooseherd/sandbox:default
    profiles: [sandbox]

volumes:
  gooseherd-pgdata:
  gooseherd-work:
  gooseherd-data:
